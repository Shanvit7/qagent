import React from 'react';
import { render } from 'ink';

// Main Ink app component
// This serves as the entry point for Ink-based UIs
export const App: React.FC = () => {
  return null; // Placeholder - will be implemented per use case
};

// Export render function for easy usage
export { render };
